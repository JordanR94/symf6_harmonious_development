{%extends 'base.html.twig'%}

{%block title%}Create a new Record | {{parent()}}{%endblock%}

{%block body%}
<h1>{{title}}</h1>

{#TODO: add an image of the record #}

<div>
    Tracks:

    <ul>
        {%for t in tracks%}
        <li>{{t.track}} - {{t.artist|upper}}</li>
        {%endfor%}
    </ul>
</div>

{%endblock%}

